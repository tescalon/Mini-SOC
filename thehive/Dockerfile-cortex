FROM thehiveproject/cortex:3.1.8
USER root

# Suppression de la source apt Amazon Corretto dont la cl̩ GPG a expir̩,
# puis installation de Python3 et des d̩pendances n̩cessaires aux analyzers.
RUN rm -f /etc/apt/sources.list.d/corretto.list \
    && apt-get update -qq \
    && apt-get install -y python3-pip \
    && pip3 install cortexutils requests Pillow pyOpenSSL python-dateutil \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*
